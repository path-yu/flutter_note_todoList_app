import "package:flutter/material.dart";

main() {
  print('ddd');
}

class MyHomePage extends StatelessWidget {
  const MyHomePage({Key key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('暂时还不知道写什么'),
      ),
    );
  }
}
